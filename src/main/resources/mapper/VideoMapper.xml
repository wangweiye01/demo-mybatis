<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.video.repository.VideoMapper">
    <resultMap id="videoMap" type="com.example.demo.video.entity.Video">
        <id property="id" column="id"/>
        <id column="url" property="url"/>
        <id column="title" property="title"/>
    </resultMap>

    <select id="findOne" resultMap="videoMap">
        SELECT title,url,id
        FROM video
        WHERE id = #{id}
    </select>
</mapper>